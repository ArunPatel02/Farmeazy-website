import React from 'react'

const Carousel = (props) => {
    return (
        <div className="carousel-item">
                  <img
                    src={props.img}
                    alt="..."
                  />
             </div>
    )
}

export default Carousel
